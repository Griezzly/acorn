# Auto-generated Prometheus configuration
prometheus.yml

# Grafana data (stored in Docker volumes, but ignore if mounted locally)
grafana-data/

# Prometheus data (stored in Docker volumes, but ignore if mounted locally)
prometheus-data/